# ── Core ML ────────────────────────────────────────────────────────────────────
# tensorflow-cpu saves ~300MB vs full tensorflow (no GPU/CUDA bloat)
tensorflow-cpu==2.15.0

# ── Audio ──────────────────────────────────────────────────────────────────────
librosa==0.10.1
soundfile==0.12.1

# faster-whisper = 4x less RAM than openai-whisper + NO PyTorch needed at all
# removing PyTorch saves ~400MB alone — biggest win
faster-whisper==1.0.3

# ── Data & Science ─────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# ── Visualisation ──────────────────────────────────────────────────────────────
matplotlib==3.9.0
plotly==5.22.0

# ── Web App ────────────────────────────────────────────────────────────────────
# 1.40.2 = last version without CSRF upload bug
streamlit==1.40.2

# ── PDF report ─────────────────────────────────────────────────────────────────
fpdf2==2.7.9

# ── Misc ───────────────────────────────────────────────────────────────────────
Pillow==10.3.0
huggingface_hub==0.23.4
